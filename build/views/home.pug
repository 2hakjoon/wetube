extends layouts/main.pug

include mixins/videoBlock

block content
    .home-videos
        each item in videoList
            +videoBlock({
                    id:item.id,
                    title:item.title,
                    views:item.views,
                    videoFile:item.fileUrl
            })