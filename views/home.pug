extends layouts/main.pug

include mixins/videoBlock

block content
    .videos
        each video in videoList
            +videoBlock({
                id:video.id,
                title:video.title,
                views:video.views,
                videoFile:video.fileUrl,
            })